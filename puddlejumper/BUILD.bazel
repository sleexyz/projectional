load("//:coarse.bzl", "dir_rule")

dir_rule(
    name = "puddlejumper",
    srcs = glob(
        ["**/*"],
        exclude = [
            "BUILD.bazel",
            "target/**/*",
            ".gitignore",
        ],
    ),
    build_cmd = "cargo build --release",
    deps = [
        "//tree-sitter-puddlejumper",
    ],
)
