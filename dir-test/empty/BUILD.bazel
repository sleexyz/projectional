load("//:rules_local.bzl", "dir_run", "dir_step")

dir_run(
    name = "test",
    cmd = """
        echo $BUILD_WORKSPACE_DIRECTORY
    """,
    deps = [
        "no_op",
    ],
)

dir_step(
    name = "no_op",
    cmd = "true",
)
