load("//:rules_local.bzl", "local_run", "local_step")

local_run(
    name = "test",
    cmd = """
        echo $BUILD_WORKSPACE_DIRECTORY
    """,
    deps = [
        "no_op",
    ],
)

local_step(
    name = "no_op",
    cmd = "true",
)
